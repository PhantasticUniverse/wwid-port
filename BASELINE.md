# Baseline / Oracle

Oracle behavior: **WIDesigner v2.6.0** (official release package)

- Release tag: `v2.6.0`
- Release date: Dec 7, 2020
- Oracle source: `oracle/v2.6.0/` (downloaded/extracted locally; ignored by git)
- Source reference snapshot: `WWIDesigner-2.6.0-src/` (tag snapshot, read-only)

Golden fixtures:
- Scenarios live in `golden/scenarios/`
- Expected oracle outputs live in `golden/expected/`
- Fixtures are generated by `golden-harness/` running against the oracle release package.